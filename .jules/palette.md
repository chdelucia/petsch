## 2026-04-17 - [Pet of the Day Empty State]
**Learning:** Providing an explicit empty state with an icon and helpful message in the 'Pet of the Day' drawer prevents a 'dead end' UX when users remove all items.
**Action:** Use conditional `@if/@else` blocks in list-based drawers to display a friendly empty state UI when the collection is empty.
